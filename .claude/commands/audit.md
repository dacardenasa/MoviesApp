Your goal is to update any vulnerable dependencies.

Do the following:

1. Run `yarn audit` to find vulnerable installed packages in this project.
2. Run `yarn-audit-fix` to apply updates.
3. Run tests and verify the updates didn't break anything.